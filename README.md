# AWS S3 Bucket Generator

-   A Simple CLI tool to generate AWS S3 Buckets using the AWS SDK for JavaScript.

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```
